# ============================================================
# Resonova — Colab / Local Requirements (Python 3.12 compatible)
# ============================================================
# Python 3.12 NOTE:
#   torch==1.12.1+cu113  → no Py3.12 wheel → use pre-installed or torch>=2.0
#   numpy==1.23.5         → no Py3.12 wheel → use numpy<2.0 (1.26.x on Py3.12)
#   librosa==0.8.1        → no Py3.12 wheel → use librosa>=0.10.0
#   opencv-python==4.5.5.64 → may lack Py3.12 wheel → use latest compat
#
# Wav2Lip compatibility is handled by patching its source code for
# numpy 1.24+ (np.bool/np.int/np.float removed), NOT by pinning numpy.
# ============================================================

# ----- PyTorch (Colab / local — pre-installed on Colab, installed here for local) -----
# On Colab: skip this block (pre-installed PyTorch 2.x already has CUDA)
# On local GPU machine: uncomment and set the right CUDA version
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# ----- Core numerical (Python 3.12 compatible) -----
numpy<2.0                  # 1.26.x on Py3.12; DO NOT pin to 1.23.5 (no Py3.12 wheel)
scipy>=1.11.0

# ----- Audio stack -----
librosa>=0.10.0            # 0.8.1 has no Py3.12 wheel; 0.10.x is compatible
soundfile>=0.12.1
audioread>=3.0.0
pydub>=0.25.1
ffmpeg-python>=0.2.0

# ----- Vision -----
opencv-python>=4.8.0       # 4.5.5.64 has no Py3.12 wheel

# ----- ASR -----
openai-whisper>=20231117

# ----- Translation -----
transformers>=4.35.0
sentencepiece>=0.1.99
sacremoses>=0.0.53
indic-nlp-library>=0.91
mosestokenizer>=1.2.1

# ----- Voice Cloning -----
TTS>=0.22.0

# ----- Lip Sync (face detection for Wav2Lip subprocess) -----
face-alignment==1.4.1      # 1.3.5 requires torch<2; 1.4.1 works with torch 2.x

# ----- Evaluation -----
sacrebleu>=2.3.1
resemblyzer>=0.1.3
jiwer>=3.0.3

# ----- UI & Visualization -----
gradio>=4.19.2
matplotlib>=3.7.0
Pillow>=9.5.0

# ----- Testing -----
pytest>=7.4.4
pytest-timeout>=2.2.0

# ----- Misc utilities -----
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0
pyworld>=0.3.4
